services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - TRIAL_MODEL=deepseek/deepseek-v4-flash
      - FREE_TRIAL_LIMIT=10
      - DAILY_LLM_LIMIT=1000
    restart: unless-stopped
